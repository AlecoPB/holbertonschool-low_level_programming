#include <stdio.h>
/**
 *_strcpy - Copies a string to another
 *@dest: string to be copied to
 *@src: source string
 *Return: I actually don't know
 */

char *_strcpy(char *dest, char *src)
{
while (*src)
{
*dest = *src;
++src;
++end;
}
*dest = *src;
}
